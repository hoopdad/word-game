# Infrastructure Scaffold

This directory is reserved for infrastructure-as-code assets.

Ownership boundary:
- Platform and deployment configuration only.
- Application feature logic belongs under `apps/*`.
